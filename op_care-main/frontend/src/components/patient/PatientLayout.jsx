import React from "react";
import Profile from "../profile";

// Wrapper to keep a dedicated patient folder entry point
const PatientLayout = () => {
  return <Profile />;
};

export default PatientLayout;
